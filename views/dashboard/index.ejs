<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta http-equiv="X-UA-Compatible" content="IE=edge">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>Product Dashboard - User</title>
	<!-- bootstrap cdn -->
	<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/css/bootstrap.min.css">
	<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/js/bootstrap.bundle.min.js"></script>
</head>
<body>
	
	<div class="container">
		<header class="d-flex justify-content-between align-items-center border-bottom">
			<div class="d-flex align-items-center">
				<h1 class="d-inline-block">V88 Merchandise</h1>
				<a class="mx-2" href="/dashboard">Dashboard</a>
				<a href="/users/edit">Profile</a>
			</div>
			<div>
				<a href="/logoff">Log Off</a>
			</div>
		</header>

		<table class="table table-striped">
			<thead class="table-primary">
				<td>Name</td>
				<td>Inventory Count</td>
				<td>Quantity Sold</td>
			</thead>
			<tbody>
	<% if(data) { %>
		<% for(let i=0; i<data.length; i++) { %>
			<% for(let key=0; key<data[i].products.length; key++) { %>
				<tr>
					<td><a href="/products/show/<%= data[i].user_id + ":" + key %>"><%= data[i].products[key].product_name %></a></td>
					<td><%= data[i].products[key].inventory_count %></td>
					<td><%= data[i].products[key].quantity_sold %></td>
				</tr>
			<% } %>
		<% } %>
	<% } else {%>
			<h3>No Products!</h3>
	<% } %>
			</tbody>
		<table>

	</div>

</body>
</html>